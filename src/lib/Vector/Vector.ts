/**
 * ## Vector
 * This is a cartesian point object. It stores 3 dimensional values.
 * Use `VectorMath` library to do operations.
 * ```
 * import {VectorMath} from "./Vector"
 * ```
 */
class Vector {
  public x: number;
  public y: number;
  public z: number;
}
